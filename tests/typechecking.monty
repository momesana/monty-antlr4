let value := True
value is Bool

# Check for reference equality/inequality.
x == y
x /== y

# Using operator `is' on values, yields its type.
1 is Int

# Types are values too. Their type is the built-in class `Type'.
Int is Type